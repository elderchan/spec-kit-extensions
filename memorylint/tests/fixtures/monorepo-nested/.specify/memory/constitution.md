# Constitution

## Module Boundaries

- Each package lives in `packages/` and is self-contained.
- Packages must not import from sibling packages directly.

## Design Patterns

- Use component-based architecture for the frontend.
- Keep business logic out of UI components.

## Code Conventions

- All public APIs must include type annotations.
- Prefer functional components over class components.
