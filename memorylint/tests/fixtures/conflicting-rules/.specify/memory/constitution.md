# Constitution

## Commit Message Policy

- Commit messages should follow the project's custom format: [JIRA-ID] description.
- Example: `[PROJ-123] Add user authentication module`.
- Never use type prefixes like `feat:` or `fix:` in commit messages.
