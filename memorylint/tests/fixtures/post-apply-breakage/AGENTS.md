# Agent Rules

## MemoryLint Integration

- Run `speckit.memorylint.run` before every planning phase.
- The audit hook triggers `speckit.memorylint.run` automatically.

## Git Workflow

- Use focused commits with Conventional Commit style.
- Do not force-push to `main`.

## Validation

- All extensions must pass their regression tests before release.
