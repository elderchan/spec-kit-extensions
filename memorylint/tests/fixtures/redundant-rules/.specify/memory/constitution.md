# Constitution

## Command Conventions

- All commands use the naming convention `speckit.{extension-id}.{command-name}`.

## Module Boundaries

- Each extension lives in its own top-level directory.
